<div class="clients-page">
  <div class="form">
    <h2>Edit Client</h2>
    <%= render 'form' %>
    <div class="mt-4">
      <%= link_to "Back", clients_path(location: @location_param) %>
    </div>
    <%= button_to "Delete Client", client_path(location: @location_param), method: :delete, form: { data: { turbo_confirm: "Are you sure you want to delete this client?" } }, class: "delete-btn" %>
  </div>
</div>